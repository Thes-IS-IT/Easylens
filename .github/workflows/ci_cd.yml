name: EasyLens Automated CI/CD & Docker Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  # -------------------------------------------------------------
  # Job 1: Static Analysis & Unit Tests
  # -------------------------------------------------------------
  analyze_and_test:
    name: 🔍 Analyze & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      - name: Create Mock .env File
        run: touch .env

      - name: Run Flutter Analyzer
        run: flutter analyze --no-fatal-warnings --no-fatal-infos

      - name: Run Flutter Unit Tests
        run: flutter test

  # -------------------------------------------------------------
  # Job 2: Docker Build & Container Automated Verification
  # -------------------------------------------------------------
  docker_build:
    name: 🐳 Docker Container Build & Verify
    needs: analyze_and_test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: easylens-web:latest
          load: true

      - name: Test Docker Container Health
        run: |
          docker run -d -p 8080:80 --name test-easylens easylens-web:latest
          sleep 5
          curl -f http://localhost:8080/ || exit 1
          docker stop test-easylens

  # -------------------------------------------------------------
  # Job 3: Android APK Automated Build Artifact
  # -------------------------------------------------------------
  build_android:
    name: 📱 Build Android APK
    needs: analyze_and_test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      - name: Create Mock .env File
        run: touch .env

      - name: Build Android APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: easylens-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: warn
